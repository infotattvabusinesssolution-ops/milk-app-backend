import mongoose from 'mongoose';
const schema=new mongoose.Schema({customer:{type:mongoose.Schema.Types.ObjectId,ref:'User',required:true},partner:{type:mongoose.Schema.Types.ObjectId,ref:'User'},subscription:{type:mongoose.Schema.Types.ObjectId,ref:'Subscription',required:true},product:{type:mongoose.Schema.Types.ObjectId,ref:'Product',required:true},deliveryDate:{type:Date,required:true,index:true},quantity:Number,slot:String,addressSnapshot:Object,payment:{type:mongoose.Schema.Types.ObjectId,ref:'Payment',required:true},status:{type:String,enum:['scheduled','assigned','picked_up','out_for_delivery','delivered','failed','rescheduled','cancelled'],default:'scheduled'},otp:String,proofUrl:String,failureReason:String,deliveredAt:Date},{timestamps:true});
schema.index({subscription:1,deliveryDate:1},{unique:true});
export const Delivery=mongoose.model('Delivery',schema);
